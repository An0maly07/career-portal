/* ===================================
   CONFIG.EXAMPLE.JS — Template for API Keys
   Copy this file to config.js and replace the placeholder values
   with your actual API keys. Never commit config.js to version control.
   =================================== */

window.AppConfig = {
    youtubeApiKey: 'YOUR_YOUTUBE_API_KEY_HERE',
    geminiApiKey: 'YOUR_GEMINI_API_KEY_HERE',
};
